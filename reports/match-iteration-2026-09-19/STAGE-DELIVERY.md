# 阶段交付：匹配算法当前状态

当前代码已有完整实现，未部署或推送本轮改动。当前不满足全部验收，不能标记目标完成。

## 已验证结果

- 匹配回归测试：279项通过；类型、修改文件lint、diff检查通过。
- 生产构建：隔离目录 `/tmp/nodeust-match-build-20260919-current` 中 `npm run build` 退出0，完成standalone资源准备。日志：`isolated-build.log`。首次隔离副本遗漏hooks，补齐后重新完整构建通过。
- 原500学生、3746帖子语料：各类返回前五准确率100%；物品类召回57.37%，低于60%门槛；其余类别原语料召回100%。这些是已暴露合成数据，不能推断真实用户效果。
- 旧150例开发集：49/60正例找回，90个拒绝/不确定样例无high误配。各类正例：换宿10/12、物品10/12、学习11/12、交通9/12、社交9/12。
- 新30例：首次10/15正例找回；用于修复后15/15，15个反例无high误配。首测与后测都保存；由实现者编写，已暴露，非独立验收。

## 本阶段实质变化

普通社交邀请不再强制填写未声明的人数限制；明确名额仍检查人数。增加具体Minecraft Java生存活动及命名Discord频道匹配。完善单人表达、零行李、每人费用、同伴学习的否定收费表达、换宿房型方向及全学年表达。页面补充缺币种的本人帖子编辑提示和对方确认说明。

## 未完成及下一步

1. 原物品召回缺口：8,030个学生/候选组合都停在currency待确认；177个正价格帖子无明确币种。未默认HKD、未改标签、未降低门槛。
2. 新独立作者验证未完成；当前开发指标不能证明泛化达标。
3. 当前版本真实MySQL/API、移动及桌面浏览器、编辑/删除/屏蔽/过期/分页端到端验收未重跑。构建通过不替代这些检查。
4. 本轮候选未上线，未推送GitHub。若继续，应先完成独立及集成验证，再决定发布。

## 代码与回退证据

- 当前匹配源码快照：`work/match-snapshots/checkpoint-20260919-stage-delivery/`，清单按实际文件内容记录SHA-256，包含未提交实现与合成评估输入，不含生产密钥或数据库。不是完整数据库备份。
- 隔离完整应用副本及构建：`/tmp/nodeust-match-build-20260919-current`。临时目录不是持久发布备份。
- 原线上版本BUILD_ID：`s_N0AgjqzUmDaEED5XFpr`；原发布备份：`/home/ubuntu/nodeust-backups/20260916T183515Z-before-email-auth/candidate-next`。
- 本次构建曾误在项目根目录启动并清理运行构建目录，已停止、从同版备份恢复并重启；登录页和其10个静态资源返回200。详见`build-incident.md`、`build-recovery-assets.json`。未改数据库或环境配置，不声称完全没有用户影响。

用户提出额度约束后，本阶段停止扩展新实验，优先交付以上代码与证据。无法读取账户剩余额度或85%使用阈值，因此不能保证具体额度百分比。

## 隔离运行补充验证

隔离standalone在127.0.0.1:3197、显式NODE_AUTH_MODE=email下启动成功，登录页及全部10个引用静态资源HTTP200，证据standalone-smoke.json。初次未指定认证模式时跳向未配置的legacy网关而404，修正测试环境后通过。测试进程已停止；未使用生产数据库、未部署。此项只验证构建可启动和公开登录页资源，不能代替已登录匹配API或真实浏览器验收。

## 验收工具补充修正

评估器现尊重每份场景声明的now，并允许MATCH_EVAL_NOW显式指定原语料评估时间；历史默认仍为9月11日，复跑旧语料指标不变。新场景按9月19日评估，调试后15/15、0反例high。证据iteration-time-audit/。此脚本修正晚于阶段源码快照；当前工作树包含它。

旧API/browser脚本仍依赖legacy代理身份头和旧测试库命名，需适配邮箱会话后才能用作当前认证系统的验收。尚未完成适配或真实API运行，不以旧脚本存在证明已验证。

## 邮箱会话与匹配API检查

复用受限nodeust_email_test测试库和file邮件模式，在隔离候选3104端口执行邮箱集成测试，19项通过，无外部邮件。新增验证：未带会话的伪造legacy代理身份头访问/api/matches返回401；真实注册验证登录的邮箱会话访问返回200、private/no-store，且新账号ownPostCount/total为0、不继承其他账号数据。日志email-match-integration.log。隔离进程已停止。

此验证只证明匹配API的邮箱会话接入与空账号隔离，尚不证明500学生推荐结果、分页、编辑/过期/屏蔽或浏览器行为。测试脚本修改晚于阶段快照。

## 有内容的邮箱匹配API

邮箱集成现20项通过。新增受限测试库中的27个monitor出售候选，真实邮箱登录账号经POST /api/posts发布购买需求；GET /api/matches?kind=goods两页分别25/2条，ID完整无重复、ownPostId正确、high置信度、响应不含ownerId/identityId/email/fullName/contactValue。数据库直接将一条涨价至超预算后总数27→26，移除另一条后26→25，验证每次请求重新核对内容和可见性。测试数据finally清理，测试进程停止，无外部邮件，日志email-match-content.log。

范围限制：单类27条，不代替500学生全量逐角色验证；直接数据库修改不证明网页编辑流程；屏蔽、过期、移动/桌面浏览器仍需单独验证。

## 屏蔽与账号停用的API重验证

扩展原有20项邮箱集成中的有内容匹配检查：候选作者停用后25条推荐变0，恢复active后回到25；本人屏蔽对方、对方屏蔽本人各自立即变0，解除后回到25；会话整体blocked也变0。整个集成重新通过，证据email-match-block.log。状态通过受限测试库直接写入，验证API读取与缓存重验证，不等同于浏览器屏蔽按钮流程。测试会话和帖子已清理、隔离进程停止，无外部邮件。

## 桌面与手机浏览器基本交互

真实邮箱验证登录得到的会话，在隔离Chromium桌面1365×900和手机390×844视口直达/matches，均显示25条首页、2条次页，上一页恢复25条，刷新后保持可用。无pageerror、无document横向溢出。证据email-browser/summary.json、desktop.png、mobile.png、integration.log。运行MATCH_EMAIL_BROWSER=1的20项邮箱集成全通过，测试进程已停止。

仅覆盖合成物品需求和分页/刷新；手机视口模拟并非真实iOS/Android设备，未验证全部类别、详情/聊天/编辑操作、全量500学生。测试辅助文件与集成脚本变化晚于阶段快照。

## 超过100条窗口的旧需求

隔离测试把本人购买需求和27条出售候选设为30天前，再为本人和候选作者各插入101条更新的无关帖子。邮箱匹配API返回本人103条有效帖子统计，仍完整找回27候选并正确分页；双视口直达、翻页和刷新同步重跑通过。20项邮箱集成全通过，日志email-match-window.log。测试数据finally清理、服务停止。此项覆盖旧物品需求跨100条窗口，不等同于所有类别500学生完整回放。

## 币种待确认API验证

对真实邮箱账号的购买帖移除HKD后，明确匹配27→0；possible=1仍返回27条，卡片数据均标记possible且missing包含currency，本人needsDetails准确指出该帖子缺币种。补回HKD后明确匹配恢复27。扩展后的20项邮箱集成通过，日志email-match-currency.log。修改经隔离数据库执行，未覆盖浏览器编辑提交；不改变原语料57.37%的未达标结论。测试服务与数据已清理。

## 同日期过期对照

隔离API将双方全部27组需求写为香港明天12:00、同一交收地点，返回27；双方一起改为香港昨天12:00，明确和包含possible的查询均返回0。双方日期始终一致，排除了仅因日期冲突而归零的解释。使用真实系统时间，不改线上时钟。20项邮箱集成通过，证据email-match-expiry.log，测试数据与进程清理。范围为物品交收过期，不代表全部类别完整回放。

## 500学生全量邮箱API回放

新增email-full-replay.mjs：受限测试库中500个合成verified账号和哈希会话、3746帖子，以当前真实时间逐人比对独立运行的当前引擎与真实/api/matches。500/500通过，821分页请求，12716条high推荐；顺序、总数、分页唯一性、本人帖子计数与关联、隐私字段检查通过。证据email-full-replay/summary.json、run.log。finally删除本轮帖子/账号/会话/用户，隔离服务停止。

这是实际邮箱会话集成一致性，不是500次注册、不独立于同一算法，也不覆盖当前日期已过期的活动语义质量。原历史语料验收分数未改变。脚本晚于最近快照。

## 切换器回退测试与候选只读预检

现有switch-release.py在临时目录下成功切换与健康失败自动回退两种测试通过；校验构建身份、事务状态、旧环境和数据标记保留，systemctl及健康请求均用测试替身，未操作真实服务。真实候选执行--check通过，确认当前邮箱模式下候选认证路由与standalone完整性。证据release-rollback-test.log、release-preflight.json。该模拟演练加只读预检不等同于候选真实发布/恢复演练，候选仍未部署。

独立作者评估需新代理，当前协作规则要求用户明确授权；已提出一次性评估选项并说明额外额度。尚未启用，现有开发集不冒充独立验证。

## 现有项目检查

当前工作树npm run check:unit通过10/10；npm run check:management通过22/22，命令退出0。日志project-unit.log、project-management.log。此项与279项匹配回归分别计数，不重复计入独立质量评估。
