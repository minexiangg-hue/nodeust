# Rule-based matching v5 release — 2026-09-17 (Asia/Shanghai)

The user approved this staged release after reviewing known matching limitations,
conditional on reversible deployment and independent algorithm development.
General-language recall gates remain failed; successful deployment does not
change that evaluation. Local model experiments are not used by the application.

- Built source: `5bfef2fc046b22dda26b86b3ac8ef5f0fb5ba7c5` (matching checkpoint `e0be78d`).
- Matching version: `reciprocal-intents-v5`.
- New build: `IouLLHXKR6y69i-M8J74s`.
- Previous build: `K-J2oPFCmJQOkDipbss3_`, transport release based on `f32c923`.
- No database migration or production test-data writes.
- Protected backup: `/home/ubuntu/nodeust-backups/20260916T181127Z-matching-v5`.
  Includes old/new builds, source archive, environment snapshot, SQL backup and
  verification records. Secrets and database backups are not committed to Git.

## Verification

165 matching tests, 10 unit checks, 22 management checks and a fresh standalone
production build passed. The fresh build passed all 17 isolated lifecycle checks.
The matching pause switch returned its disabled state while posts remained usable;
unauthenticated matching access still returned 401. The saved previous build
booted successfully against the isolated database. The switcher's normal path and
failed-health automatic recovery were exercised with temporary fixture builds;
production was not deliberately rolled back after the successful release.

After deployment, localhost health reported database reachable. Authenticated
read-only `/api/matches` returned v5 with private/no-store caching and no private
identity fields; `/matches` returned 200. Public homepage and sampled new-build
static assets returned 200. The public health URL returned 401 without gateway
authentication; the authenticated internal service check passed.

## Roll back / roll forward

From `/home/ubuntu/nodeust`, restore the saved previous application:

```bash
python3 scripts/switch-release.py /home/ubuntu/nodeust-backups/20260916T181127Z-matching-v5/previous-next --check
python3 scripts/switch-release.py /home/ubuntu/nodeust-backups/20260916T181127Z-matching-v5/previous-next
```

Return to this release using the same switcher and the backup's `candidate-next`
path. Both actions retain the displaced build and leave current user data and
configuration intact. Do not restore the SQL dump for an ordinary code rollback.
Runtime build IDs, rather than the source checkout alone, identify the serving
version after a rollback. Future database migrations need their own compatibility
review before promising the same behavior.

For algorithm-only work, edit/test `lib/match/`, keep the service/API contract,
version the changed algorithm, and deploy a separately built checkpoint. Existing
posts need not be rewritten. To pause matching, set `NODE_MATCHING_ENABLED=false`
in the service environment and restart; posts and chat remain available.
