# Domain HTTPS activation — 2026-09-17

User explicitly authorized production HTTPS and SMTP configuration.

- https://nodeust.duckdns.org/ returns 200 with normal certificate validation.
- HTTP redirects with 308 to the canonical HTTPS domain.
- Let’s Encrypt certificate expires 2026-12-16. Certbot timer is active.
- Targeted Certbot staging renewal and deploy-hook execution passed (`--dry-run --no-random-sleep-on-renew --run-deploy-hooks`).
- Deploy renewal hook tests and reloads nginx at `/etc/letsencrypt/renewal-hooks/deploy/nodeust-nginx-reload`.
- Old gateway on 8081 remains active; no new authentication cutover or database migration.
- Active nginx configuration is a **regular file** `/etc/nginx/sites-enabled/nodeust-preview.conf`, not a symlink to sites-available. The initial ACME attempt edited the inactive sites-available copy and failed with 401; correcting the active file resolved validation.
- Pre-change active/inactive configs are separately backed up in `/home/ubuntu/nodeust-backups/20260917-https/`. The auth rollback template preserves HTTPS.
- Other named virtual hosts were not edited.

## SMTP pending

HKUST Connect documents `smtp.office365.com`, port 587, STARTTLS, **OAuth2**:
https://itso.hkust.edu.hk/services/general-it-services/communication-collaboration/email/connect/modern_auth

An email address alone is insufficient. The current mail adapter supports password-authenticated SMTP; using Connect requires an OAuth2 adapter and an authorized Microsoft application with token renewal. School tenant/mailbox permissions must be confirmed first. Do not request or store a university login password as an SMTP password. No external mail has been sent. No sender or Owner personal address is recorded in tracked configuration.
